apiVersion: v1
kind: Service
metadata:
  name: { { include "aemeath.fullname" . } }
  labels: { { - include "aemeath.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "aemeath.bot.selectorLabels" . | nindent 4 } }
