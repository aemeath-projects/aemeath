apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aemeath.configMapName" . }}
  labels:
    {{- include "aemeath.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
